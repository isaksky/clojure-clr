# Plan: Restore Modern .NET AOT Compilation

## High Level Strategy

Use the current `PersistedAssemblyBuilder` path for the first implementation milestone, because the minimal namespace already compiles, source-free loads, and verifies. Keep progressive Clojure load/eval semantics by making eval and persisted generation explicit peers: eval code must remain runnable during compilation, persisted code must remain independent of temporary eval-only types, and every generated type/member reference must resolve through a backend-aware identity map. The Cecil checkpoint has been reassessed after paired generation and remains deferred: current blockers are feature-specific pairing/policy gaps, not a general failure of `PersistedAssemblyBuilder`.

## Recommended Steps

- [x] Confirm and document the exact first milestone scope before implementation. Use [Goal.md](Goal.md), [current-aot-behavior.md](results/current-aot-behavior.md), [backend-strategy-decision.md](results/backend-strategy-decision.md), and [compiler-change-list.md](results/compiler-change-list.md). Decision checkpoint: direct linking is disabled for the first pass with `CLOJURE_COMPILER_DIRECT_LINKING=false`; backend-aware direct-link records are deferred until after the minimal paired-generation path is green.

  First milestone scope: make ordinary namespace AOT reliable for top-level `def`, `defn`, source-free load, top-level `let` lowered through generated function classes, selected generated-form families needed by the runtime namespace tranche, and same-runtime `PersistedAssemblyBuilder` output. The persisted assembly must not reference transient eval-only generated types, and eval code must not depend on persisted generated types while compiling. Dynamic host interop/call-site helpers, `deftype*`, `reify*`, `gen-class`, `proxy`, debug-symbol portability, cross-target reference assembly selection, and Cecil replacement remain outside this first milestone unless a verification fixture proves they are needed earlier.

- [x] Fix the modern .NET compile-driver build path so baseline AOT can run from normal builds on Unix. Relevant files: [Clojure.Compile.csproj](../Clojure/Clojure.Compile/Clojure.Compile.csproj), [Compile.cs](../Clojure/Clojure.Compile/Compile.cs), and [current-aot-behavior.md](results/current-aot-behavior.md). Decision checkpoint: `net9.0+` post-build invokes `dotnet "$(TargetPath)"`, not the generated apphost, so Unix builds do not depend on `mono` or platform-specific apphost naming.

- [x] Turn the current minimal AOT scenario into a repeatable regression test. Relevant context: [aot-verification-plan.md](results/aot-verification-plan.md), [current-aot sample](experiments/current-aot/sample/ns.clj), [AssemblyInspector](experiments/current-aot/AssemblyInspector/Program.cs), [Compiler.cs](../Clojure/Clojure/CljCompiler/Compiler.cs), and [RT.cs](../Clojure/Clojure/Lib/RT.cs). Implemented as `AotRegressionTests.MinimalNamespaceAotProducesPersistedAssembly`, a `net9.0+` NUnit regression that compiles a temp namespace with `def`, `defn`, and top-level `let` while direct linking is bound off, then asserts the persisted namespace DLL and initializer are present.

- [x] Add verification gates for source-free load, reflection inspection, and `dotnet-ilverify`. Use [aot-verification-plan.md](results/aot-verification-plan.md), [cross-reference-risk-map.md](results/cross-reference-risk-map.md), and [current-aot-behavior.md](results/current-aot-behavior.md). Implemented as `AotRegressionTests.MinimalNamespaceAotPassesReflectionInspection`, `AotRegressionTests.MinimalNamespaceAotLoadsWithoutSourceInFreshProcess`, and `AotRegressionTests.MinimalNamespaceAotPassesIlVerifyWhenConfigured`. Decision: source-free load and reflection inspection are required in normal local/CI NUnit runs for `net9.0+`; `dotnet-ilverify` is an opt-in gate enabled by setting `CLOJURE_AOT_ILVERIFY` to the local `ilverify` executable path so CI can install and enforce it without making every developer machine carry a fixed SDK/tool layout.

- [x] Constrain direct linking before introducing paired generation. Relevant files: [Compiler.cs](../Clojure/Clojure/CljCompiler/Compiler.cs), [StaticInvokeExpr.cs](../Clojure/Clojure/CljCompiler/Ast/StaticInvokeExpr.cs), [InvokeExpr.cs](../Clojure/Clojure/CljCompiler/Ast/InvokeExpr.cs), [cross-reference-risk-map.md](results/cross-reference-risk-map.md), and [dual-generation-contract.md](results/dual-generation-contract.md). Decision checkpoint: on `net9.0+`, persisted assembly compilation suppresses direct linking even when `:direct-linking true` is bound. `RegisterDirectLink` and `TryGetDirectLink` now share the same persisted-context gate, and `InvokeExpr` uses the compiler gate instead of reading the raw option directly. Backend-aware direct-link records remain deferred until generated type/member identities exist.

- [x] Introduce a generated type/member identity registry. Use [dual-generation-contract.md](results/dual-generation-contract.md), [generated-type-inventory.md](results/generated-type-inventory.md), [ObjExpr.cs](../Clojure/Clojure/CljCompiler/Ast/ObjExpr.cs), [FnExpr.cs](../Clojure/Clojure/CljCompiler/Ast/FnExpr.cs), and [FnMethod.cs](../Clojure/Clojure/CljCompiler/Ast/FnMethod.cs). Implemented as `GeneratedArtifactRegistry`, `GeneratedTypeId`, and `GeneratedMemberId` attached to `GenContext`, with lightweight recording for namespace init types and generated function artifacts. Decision checkpoint: the first implementation keeps the current two-independent-analysis shape: persisted compile analysis emits the saved assembly and separate eval analysis preserves progressive semantics. The registry uses backend-local ordinals so those two passes can pair logical artifacts once the eval and persisted contexts are made explicit and shared.

- [x] Make AOT compilation carry explicit eval and persisted generation contexts. Relevant files: [GenContext.cs](../Clojure/Clojure/CljCompiler/Context/GenContext.cs), [MyAssemblyGen.cs](../Clojure/Clojure/CljCompiler/Context/MyAssemblyGen.cs), [Compiler.cs](../Clojure/Clojure/CljCompiler/Compiler.cs), [dual-generation-contract.md](results/dual-generation-contract.md), and [dual-generation-prototype.md](results/dual-generation-prototype.md). Implemented as `GenerationContextPair`, shared `GeneratedArtifactRegistry` wiring for paired eval/persisted `GenContext`s, and compiler dynamic bindings that keep the separate eval pass in the runnable eval context while persisted emit remains in the persisted context.

- [x] Route generated function class creation through the identity registry. Relevant files: [FnExpr.cs](../Clojure/Clojure/CljCompiler/Ast/FnExpr.cs), [ObjExpr.cs](../Clojure/Clojure/CljCompiler/Ast/ObjExpr.cs), [FnMethod.cs](../Clojure/Clojure/CljCompiler/Ast/FnMethod.cs), [generated-type-inventory.md](results/generated-type-inventory.md), and [progressive-semantics-map.md](results/progressive-semantics-map.md). Implemented by declaring `FnExpr` logical type identities before runtime type definition, normalizing backend-local `RT.nextID` suffixes out of generated logical names while preserving emitted runtime names, and routing `FnMethod` method creation through generated-member registration. Regression coverage now verifies a named function literal whose persisted and eval runtime names differ but share one logical artifact.

- [x] Audit and map constants, Vars, Keywords, constructors, fields, and helper methods emitted into init and function classes. Relevant files: [Compiler.cs](../Clojure/Clojure/CljCompiler/Compiler.cs), [ObjExpr.cs](../Clojure/Clojure/CljCompiler/Ast/ObjExpr.cs), [DefExpr.cs](../Clojure/Clojure/CljCompiler/Ast/DefExpr.cs), [VarExpr.cs](../Clojure/Clojure/CljCompiler/Ast/VarExpr.cs), [cross-reference-risk-map.md](results/cross-reference-risk-map.md), and [progressive-semantics-map.md](results/progressive-semantics-map.md). Implemented as [emitted-member-map.md](results/emitted-member-map.md) plus regression coverage for namespace init constants, Var/Keyword constant fields, generated function constructors, static constructors, invoke methods, and constants helpers. Decision checkpoint: the minimal path is mapped through `GeneratedArtifactRegistry`; generated `Type` constants, dynamic host interop helpers, and `deftype*`/`reify*` member families remain deferred to their existing policy beads.

- [x] Preserve progressive eval semantics with macro and later-form dependency tests. Relevant docs and files: [progressive-semantics-map.md](results/progressive-semantics-map.md), [Compiler.cs](../Clojure/Clojure/CljCompiler/Compiler.cs), [core.clj](../Clojure/Clojure.Source/clojure/core.clj), and [current-aot-behavior.md](results/current-aot-behavior.md). Implemented as `AotRegressionTests.ProgressiveMacroAotPreservesCompileTimeMacroAndLaterFormDependencies` and `AotRegressionTests.ProgressiveMacroAotLoadsWithoutSourceInFreshProcess`. Decision checkpoint: a macro whose expansion depends on an earlier top-level `def`, followed by later forms depending on the macro-produced Var, works with the current separate eval plus persisted replay model.

- [x] Decide the first-pass policy for dynamic host interop and call-site helpers. Relevant files: [DynInitHelper.cs](../Clojure/Clojure/CljCompiler/Context/DynInitHelper.cs), [MethodExpr.cs](../Clojure/Clojure/CljCompiler/Ast/MethodExpr.cs), runtime binders under [Runtime/Binding](../Clojure/Clojure/Runtime/Binding), [generated-type-inventory.md](results/generated-type-inventory.md), and [cross-reference-risk-map.md](results/cross-reference-risk-map.md). Decision checkpoint: dynamic host interop is explicitly rejected during modern .NET persisted AOT compilation for the first milestone. `MethodExpr.EmitDynamicCallPreamble` now gates DLR call-site helper/delegate emission in persisted compile contexts, and regression coverage verifies a dynamically-dispatched host call fails before a namespace DLL is saved. Pairing `DynInitHelper` helper types, delegate types, `CallSite<T>` fields, and binder-created setters is deferred until the minimal `def`/`defn`/`let`/macro path is green against the runtime namespace tranche.

- [x] Decide and enforce the first-pass policy for `deftype*`, `reify*`, `gen-class`, `proxy`, `gen-interface`, and `gen-delegate`. Relevant files: [NewInstanceExpr.cs](../Clojure/Clojure/CljCompiler/Ast/NewInstanceExpr.cs), [NewInstanceMethod.cs](../Clojure/Clojure/CljCompiler/Ast/NewInstanceMethod.cs), [GenClass.cs](../Clojure/Clojure/CljCompiler/GenClass.cs), [GenProxy.cs](../Clojure/Clojure/CljCompiler/GenProxy.cs), [GenInterface.cs](../Clojure/Clojure/CljCompiler/GenInterface.cs), [GenDelegate.cs](../Clojure/Clojure/CljCompiler/GenDelegate.cs), and [compiler-change-list.md](results/compiler-change-list.md). Decision checkpoint: `deftype*`, `reify*`, `gen-class`, and `proxy` remain explicitly rejected during modern .NET persisted AOT compilation. `gen-interface` and `gen-delegate` were initially deferred, then promoted into the runtime namespace tranche after follow-up pairing/runtime-only policy work.

- [x] Expand verification from the sample namespace to a small macro/progressive-eval fixture and then to a small runtime namespace tranche. Relevant context: [aot-verification-plan.md](results/aot-verification-plan.md), [progressive-semantics-map.md](results/progressive-semantics-map.md), [Clojure.Source](../Clojure/Clojure.Source), and [Clojure.Compile.csproj](../Clojure/Clojure.Compile/Clojure.Compile.csproj). Implemented as `AotRegressionTests.RuntimeNamespaceTrancheAotProducesPersistedAssemblies` and `AotRegressionTests.RuntimeNamespaceTrancheAotLoadsInFreshProcess`, initially covering `clojure.walk`, `clojure.template`, and `clojure.set` with persisted assembly inspection and fresh-process use from the compile output.

- [x] Pair generated-form families needed for broader runtime namespace AOT. Relevant files: [GenInterface.cs](../Clojure/Clojure/CljCompiler/GenInterface.cs), [StaticMethodExpr.cs](../Clojure/Clojure/CljCompiler/Ast/StaticMethodExpr.cs), [GenDelegate.cs](../Clojure/Clojure/CljCompiler/GenDelegate.cs), and [AotRegressionTests.cs](../Clojure/Csharp.Tests/LibTests/AotRegressionTests.cs). Implemented paired persisted/eval `gen-interface` generation with registry records for interface types and methods, and a runtime-only `gen-delegate` save policy where persisted code calls `GenDelegate.Create` without saving wrapper types. The runtime namespace tranche now includes `clojure.string` and `clojure.data` with source-free fresh-process verification.

- [x] Revisit debug symbols and target-framework policy only after the minimal paired-generation path is green. Relevant files and docs: [MyAssemblyGen.cs](../Clojure/Clojure/CljCompiler/Context/MyAssemblyGen.cs), [GenContext.cs](../Clojure/Clojure/CljCompiler/Context/GenContext.cs), [backend-strategy-decision.md](results/backend-strategy-decision.md), and [aot-verification-plan.md](results/aot-verification-plan.md). Decision checkpoint: modern persisted AOT remains same-runtime-only for the first pass by passing the executing runtime core assembly to `PersistedAssemblyBuilder`, and persisted debug document/PDB emission is disabled even in Debug builds. Cross-TFM reference assembly selection is tracked by `clojure-clr-rjb`; verified portable debug symbols are tracked by `clojure-clr-zkm`.

- [x] Reassess the need for a Cecil backend after Reflection.Emit paired generation is working. Relevant docs and references: [backend-strategy-decision.md](results/backend-strategy-decision.md), [mono-cecil-aot-generation-notes.md](docs/mono-cecil-aot-generation-notes.md), [XamlX Cecil backend](XamlX/src/XamlX.IL.Cecil), [Avalonia build task](Avalonia/src/Avalonia.Build.Tasks/XamlCompilerTaskExecutor.cs), [il-repack](il-repack/ILRepack/ILRepack.cs), [Fody](Fody/FodyIsolated), and [coverlet Instrumenter](coverlet/src/coverlet.core/Instrumentation/Instrumenter.cs). Decision checkpoint: do not start a Cecil backend for the next AOT slice. The current Reflection.Emit/`PersistedAssemblyBuilder` path now verifies the minimal namespace, macro/progressive-eval fixture, generated function/member identity mapping, source-free fresh-process loading, `gen-interface` pairing, `gen-delegate` runtime-only wrappers, same-runtime target policy, disabled persisted debug symbols, and the runtime namespace tranche through `clojure.data`. Remaining ready work is tracked as targeted beads for dynamic host interop, `gen-class` policy/tests, cross-TFM references, and portable debug symbols.

- [ ] Keep this plan live. After each decision checkpoint, update this file with the chosen path, any newly discovered blocker, and the next concrete implementation slice.
